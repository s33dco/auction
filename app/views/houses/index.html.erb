<header>
	<h1>Auction House's</h1>
</header>
<article>
	<% @houses.each do |house| %>
		<div class="house">
			<%= house.name %>	<%=link_to 'edit', edit_house_path(house) %>
		</div>
	<% end %>
</article>