<article>
	<header>
		<h1>Editing <%= @lot.make_and_model%><br>(post auction)</h1>
	</header>
	<%= form_with(model: @lot, local:true) do |f| %>
		<%= render 'lot_fields', f: f%>
		<%= f.submit %>
	<% end %>
</article>